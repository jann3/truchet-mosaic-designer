export function createLayersPanelContent(): HTMLElement {
  const placeholder = document.createElement('p');
  placeholder.textContent = 'Layers will appear here once the layer system ships (Phase 7).';
  return placeholder;
}
